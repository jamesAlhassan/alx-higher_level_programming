#!/usr/bin/python3

def multiple_returns(sentence):
    if sentence == "":
        sentence[0] == None
    else:
        length = len(sentence)
        first_char = sentence[0]
    tupl = (length, first_char)
    return tupl
